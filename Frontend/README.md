# ScreenLoop Frontend

React, TypeScript, Vite, Tailwind, and the local WebSocket bridge used by the ScreenLoop desktop shell.

Useful commands:

```powershell
npm run dev
npm run build
npm run lint
```
